<%- include('partial/header') %>

<div class="container">
    <table class="table " id="import">
        <thead class="table-primary">
            <th class="table-data">JOB NO</th>
            <th class="table-data">CONSIGNEE</th>
            <th class="table-data">HOUSE BL</th>
            <th class="table-data">MASTER BL</th>
            <th class="table-data">ON BOARD</th>
            <th class="table-data">ETA</th>
            <th class="table-data">OPTION</th>
        </thead>
        <% importList.forEach(data => { %>
            <tbody>
                <td><%= data.jobno %></td>
                <td><%= data.mblno %></td>
                <td><%= data.shipper %></td>
                <td><%= data.agent %></td>
                <td><%= new Date(data.dateonboard).toDateString() %></td>
                <td><%= new Date(data.eta).toDateString() %></td>
                <td><a class="btn" href="/import/<%= data._id %>">VIEW</a></td>
            </tbody>
        <% }) %>
    </table>
</div>

<%- include('partial/footer') %>